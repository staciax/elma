// import { t } from 'elysia';

// export const LimitOffset = t.Object({
// 	limit: t.Optional(t.Integer({ minimum: 1, default: 100 })),
// 	offset: t.Optional(t.Integer({ minimum: 0, default: 0 })),
// });

// const Limit = t.Optional(t.Integer({ minimum: 1, default: 100 }));
// const Offset = t.Optional(t.Integer({ minimum: 0, default: 0 }));
